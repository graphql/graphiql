---
'@graphiql/react': patch
---

Document the v6 OKLCH design tokens (`--bg-*`, `--border-*`, `--fg-*`, `--accent-*`, `--type-*`, `--btn-*`, `--radius-*`, `--shadow-*`) and the `typeCategory` helper in the README's theming section, and mark the legacy v5 `--color-*` HSL variables deprecated.
