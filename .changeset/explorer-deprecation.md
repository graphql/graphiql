---
'@graphiql/plugin-explorer': major
---

This package is deprecated. New consumers should use `@graphiql/plugin-query-builder` (default-installed in `graphiql`). The package emits a console warning on first use; removal is planned for v7. See the v6 migration guide for replacement guidance.
