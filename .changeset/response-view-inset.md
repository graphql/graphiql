---
'@graphiql/react': patch
---

Add a consistent 16px inset to the response result window across all three views (JSON, Tree, Table), matching the padding already used by the query and variables/headers editors.
