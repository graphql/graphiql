---
'@graphiql/plugin-query-builder': minor
'graphiql': minor
---

The query builder supports union and interface fields via inline-fragment type-condition selectors. The query builder is now default-installed in the `graphiql` meta-package — no extra setup required to access it.
