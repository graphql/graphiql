{
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "testonly": "lerna run testonly",
    "lint": "lerna run lint",
    "check": "lerna run check"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
