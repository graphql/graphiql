{
  "name": "graphiql-example-cdn",
  "version": "0.0.5",
  "private": true,
  "license": "MIT",
  "description": "An example using GraphiQL",
  "scripts": {
    "setup": "rm -rf bundle | true && mkdir -p bundle | true && cp -R ../../graphiql/bundle/min/* bundle/ && cp -R ../../graphiql/*.css bundle/",
    "start": "node server"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-graphql": "^0.9.0",
    "graphql": "^14.4.2",
    "graphiql": "file:../../graphiql"
  }
}
